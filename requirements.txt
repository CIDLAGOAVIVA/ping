# PING - UFF ANALYTICS - Dependências Python
# 
# Instalação com pip (alternativa ao uv):
#   pip install -r requirements.txt
#
# Recomendamos usar uv para melhor gerenciamento:
#   uv sync

# UI Framework
gradio>=4.0.0

# Vector Database
chromadb>=0.4.0

# LLM & Embeddings
langchain>=0.1.0
langchain-community>=0.0.10
ollama>=0.1.0  # Local embeddings only (mxbai-embed-large)
openai>=1.0.0  # DeepSeek Chat API
ddgs>=3.9.0  # DuckDuckGo web search (antigo: duckduckgo-search)

# Utilities
python-dateutil>=2.8.0
emoji>=2.0.0
